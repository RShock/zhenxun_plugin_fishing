# Minimal deps for free GitHub Actions CI (plugin unit/integration tests)
pytest>=8.0
pytest-asyncio>=0.23
pydantic>=2.0
Jinja2>=3.1
Pillow>=10.0
aiohttp>=3.9
tortoise-orm>=0.20
# Windows / minimal images need IANA tz database for ZoneInfo("Asia/Shanghai")
tzdata>=2024.1
