# ??????

## ??/????????

- ??? `??` ????????????????? QQ ??????????? 10 ????????????????????????????????
- ?????????????????????????>???????????????????? ASCII `>`?????????????????
- QQ ?????? 5 ??1~5 ?????6~10 ????????? Markdown ????????????????????
- ??? `??` ????????????????????????????????????????????????????????????????
- ??????????


## 钓鱼使用菜单与定时推送

- 群聊无参数 `钓鱼使用` 在存在 QQ 官方 Bot 主动发送通道时，显示玩家当前持有且符合使用条件的道具按钮；库存为 0、未在钓鱼、地图不符、每日打窝次数用完或对应打窝/木框/猫框层数已满时不展示。
- 选择 `UTR自选券` 后二次展示可兑换 UTR 鱼，按钮标注 `13图 鱼名` 并提交 `钓鱼使用 UTR自选券 鱼名`；最多 10 条，QQ 每条消息 5 行，因此最多两条消息。
- 菜单定时任务的数据库就绪检查必须兼容主程序未导出 `is_initialized` 的版本，不得让 ImportError 逃出 APScheduler 任务。
- 星空展示框已满级时仍要执行流星鱼奇迹结算；结算所得星空框继续累加到 `star_frames` 背包库存，不受展示框等级上限影响。
