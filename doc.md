# ??????

## ??/????????

- ??? `??` ????????????????? QQ ??????????? 10 ????????????????????????????????
- ?????????????????????????>???????????????????? ASCII `>`?????????????????
- QQ ?????? 5 ??1~5 ?????6~10 ????????? Markdown ????????????????????
- ??? `??` ????????????????????????????????????????????????????????????????
- ??????????


## 钓鱼使用菜单与定时推送

- 群聊无参数 `钓鱼使用` 在存在 QQ 官方 Bot 主动发送通道时，将玩家当前持有且符合使用条件的道具显示为按钮；已持有但因未在钓鱼、地图不符、次数用完、层数已满或功能未开放而不可用的道具，必须在菜单 Markdown 文字区列出数量与原因，不生成按钮、不占用按钮行。
- 菜单展示与实际 `钓鱼使用` 执行前检查必须统一调用 `items/use_checks.py`，不得在 Bot/非 Bot 入口分别维护拒绝条件与理由。`UseCheckContext` 应复用一次读取的玩家模型，从内存中获取背包、图鉴和每日计数，并将本次检查所需的打窝/木框/猫框/星空艇 Buff 合并为一次惰性查询后缓存。
- 选择 `UTR自选券` 后二次展示可兑换 UTR 鱼，按钮标注 `13图 鱼名` 并提交 `钓鱼使用 UTR自选券 鱼名`；最多 10 条，QQ 每条消息 5 行，因此最多两条消息。
- 菜单定时任务的数据库就绪检查必须兼容主程序未导出 `is_initialized` 的版本，不得让 ImportError 逃出 APScheduler 任务。
- 星空展示框已满级时仍要执行流星鱼奇迹结算；结算所得星空框继续累加到 `star_frames` 背包库存，不受展示框等级上限影响。
