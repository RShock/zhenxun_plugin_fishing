# S2 星穹矿脉新对话交接

更新时间：2026-08-07。

这份文件供下一次 Codex 对话直接接手。开始工作前依次阅读：

1. `DESIGN_INTENT.md`：最高优先级的玩家体验和不可回归约束。
2. 本文件：当前做到哪里、哪些结论有效、下一步做什么。
3. `s2_first_ten_days.py`：当前玩家策略与前期验收入口。
4. `s2_mining_simulator.py`：数值、科技、资源和时代实现。
5. `FIRST_TEN_DAYS_TRACE.md`：固定 seed 的最新操作明细。

`GAME_DESIGN_VNEXT.md` 是完整系统草案，但其中标记为历史基线的数值和日程不能覆盖 `DESIGN_INTENT.md` 与本文件。

## 当前任务边界

- 只设计和验证独立命令行模拟器，并把已签收规则同步到 `web/static/s2-vnext/` 独立测试网页。
- 不接主程序、数据库、QQ 指令或正式 HTML UI；测试网页不是上线界面。
- 不恢复旧 S2 代码，不使用钓鱼主世界货币、药水或成长数据。
- 每轮只解决一个可观察阶段，先打磨前十天，再拼接 D11 以后和重生循环。
- 阶段二的 `10^308` 星球数量外推暂时冻结，阶段一签收后再继续。

## 游戏目标

- 阶段一：挖矿、升级、自动化、跨时代，挖穿一颗星球并重生。
- 重生：清空本地资源与科技，获得星球核心；核心提供永久加速、阶段跳过、更多核心和额外摧毁星球等能力。
- 阶段二：逐渐把单颗星球循环压缩为自动流程，一次摧毁越来越多星球，最终处理近似“无限”的星球数量。
- 最终奖励：宇宙核心，作为返回钓鱼主世界并进入 S3 的钥匙。

理想终值是单星球深度和阶段二星球数都可表达至 `10^308`，但玩家体验优先于强行对齐该数值。通关天数没有硬限制。

## QQ 群核心规则

- 每个自然日最多三条成功的手动升级消息，不限制查看、挖矿结算和状态查询次数。
- 一条升级消息可以包含多个项目和不同数量，例如 `升级 矿镐=2 矿车=1`。
- 任意本地科技累计手动升到 3 级后，永久解锁该科技的自动采购；重生后仍有效。
- 自动采购不消耗 QQ 消息，也不应逐条通知玩家，正式展示按小时或按日聚合。
- 模拟中的活跃玩家每小时检查一次，但只在值得升级时消耗消息。不能把三条消息固定成三个 8 小时窗口。

## 资源职责

- 矿币：通用本地科技货币，由矿物精炼获得。
- 锡矿：基础工程材料。
- 铜矿：工业时代材料。
- 紫晶：电力时代材料。
- 金猫锭：现代与高阶设备材料。
- 虹核晶：未来、行星和异常科技材料。
- 星球核心：重生后的永久科技货币。

这些资源都必须参与成本或转化，不是顶部状态栏的装饰数字。资源种类保持在可理解范围内，不继续无目的扩张。

## 当前模拟状态

当前模型采用 10 分钟固定结算；活跃、普通、低频玩家分别按每小时、每 3 小时和每天 3 次查看，每天都最多发送三条成功升级消息。手动决策依据上次真实购买后的观察时间、资源储备、当天剩余查看机会和科技价值动态保留消息，不绑定固定窗口。时代同时受深度和前一时代科技掌握度限制。

固定 `seed=42` 的 D1～D10 已签收结果：

| 观察点 | 当前结果 |
|---|---|
| D1 第一次升级 | 01:00，矿镐 `+1` |
| D1 消息时点 | 01:00、06:00、14:00 |
| 工业时代 | D5 首次进入 |
| D10 | 仍处于工业时代，16/124 节点 |
| 前十天手动数量 | `+1×15`、`+2×14`、`+3×1` |
| 唯一直接 `+3` | D8 07:00，爆破镐 `+3` |

三种画像在 `seed=42` 下均于 D5 进入工业时代，D10 均保持工业时代并触达 16/124 节点；D10 进度差异只反映查看频率带来的路线效率。额外检查 `seed=1、7、42、99、2026` 时，工业时代均在 D5～D6，D10 均未进入电力时代。

## 尚未签收的问题

- D11 以后、电力时代约 D11～D14 的稳定性尚未按三画像和多 seed 正式签收。
- D19 进入现代时代只是历史观测，不是已签收目标；后续时代和首次爆星日期仍需重新校准。
- 旧的 D30/D45 测试来自更快曲线。它们继续失败时不要为变绿而重新加速前十天，应在 D11～首次爆星签收后重写长线验收窗口。
- 独立网页已同步前十天规则，但尚未承担自动玩家策略回放；按钮操作用于人工验证额度、成本和时代门槛。

## 当前验证结果

2026-08-07 使用固定 `seed=42` 重新生成了 `FIRST_TEN_DAYS_TRACE.md`，活跃画像的完整体验闸门与三画像矩阵审计均通过。新增测试覆盖首次升级等待、日内消息保留、每天最多一次直接 `+3`，以及三画像共同的工业时代阶段结构。

关闭仓库级第三方 pytest 自动加载后，模拟器测试结果为 `10 passed / 3 failed`。失败项仍是明确保留的旧长线断言：

- 旧测试要求 D30 前完成首次重生，当前 D30 尚未重生。
- 旧多 seed 矩阵仍要求 15～45 天首次重生和全部节点覆盖。
- 旧 D45 集成测试要求 124/124 节点，当前 seed=42 为 82/124。

这些失败是前期减速后的预期遗留，不是 D1～D10 回归。不要直接删除断言，也不要为让旧测试变绿而提高前十天速度。

## 下一步顺序

1. 保持 D1～D10 已签收曲线，不再为旧 D30/D45 断言调整前期。
2. 从 D11 开始审查电力时代进入、工业时代停留时长和三画像差异。
3. 按相同方法逐段扩展到现代、未来与首次爆星，并重新定义长线验收窗口。
4. 首次重生签收后再拼接多次重生、核心科技和阶段二。

每次调参都要重新生成 `FIRST_TEN_DAYS_TRACE.md`，记录模拟结果和结论；不能只看最终深度。

## 常用命令

从仓库根目录运行前十天回归：

```powershell
.venv\Scripts\python.exe zhenxun/plugins/zhenxun_plugin_fishing/doc/s2设计/星穹矿脉/vnext/s2_first_ten_days.py --days 10 --seed 42
```

运行当前单元测试：

```powershell
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD = '1'
.venv\Scripts\python.exe -m pytest zhenxun/plugins/zhenxun_plugin_fishing/doc/s2设计/星穹矿脉/vnext/test_s2_mining_simulator.py -q
```

如果不设置 `PYTEST_DISABLE_PLUGIN_AUTOLOAD`，当前仓库的 `nonebug` 插件会在收集这些独立模拟测试前因 NoneBot 环境不兼容而报错。

若 Windows 下 `apply_patch` 报路径或补丁末行错误，阅读插件根目录的 `tools/APPLY_PATCH_WINDOWS.md`。
